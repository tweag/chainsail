apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "chainsail.fullname" . }}-test-connection"
  labels:
    {{- include "chainsail.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "chainsail.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
